import React from "react";
import styles from "./footer.module.css";

const Footer = (props) => {
  return (
    <>
      <footer className={styles.footer}>Make Your Deck Card</footer>
    </>
  );
};

export default Footer;
